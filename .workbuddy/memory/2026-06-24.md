# 2026-06-24 工作日志

## GitHub 代码同步
- 本地仓库已存在，远程指向 `https://github.com/wbzzzd/UXO_v1.git`
- 本地有 `main` 和 `minimax_temp` 两个分支
- `minimax_temp` 比 `main` 领先5个提交（UI重构、3D场景、数据驱动架构等）
- 用户选择将 `minimax_temp` 合并到 `main`
- 已成功 Fast-forward 合并并推送到 GitHub
- 合并后 main 分支新增: 50个文件变更，5821行新增代码
- 新增模块: GlobalStyle, MockDataGenerator, AirportSceneFactory, AirportData, 以及多个 MainWindow 子组件

## 排弹系统现状评审 + 开发策略制定

### 评审过程
- 创建 `software-uxo-review` 团队，产品经理和架构师分别独立评审当前代码
- PM 结论：当前是"展厅式开发"，UI 90% 但业务逻辑 0%，覆盖 SRS 约 10% P0 需求
- 架构师结论：综合评分 ⭐⭐½，3 个 P0 问题（业务模块缺失/3D不支持增量更新/Application stub）+ 6 个 P1 问题

### 硬件缺位策略
- 用户提出所有硬件设备均未到位，需要讨论提前做软件开发的策略
- PM 分析：69 个子功能中 52% 零硬件依赖可独立开发，20% 弱依赖部分可行，28% 强依赖需等硬件
- 架构师设计 HAL 抽象层方案：IDevice→ISensor/IActuator 接口树 + 仿真设备层 + 数据录制回放
- PM 补充前端策略：FrontendDataStore 单例 + MQTT Adapter + 信号驱动 UI 刷新

### DDR 机制建立
- 用户要求讨论结论固化成文档，建立了 DDR（开发决策记录）机制
- 位置：`docs/ddr/`，每个决策一个编号文件
- 已创建：DDR-001（现状评审结论）、DDR-002（硬件缺位策略）、DDR-003（Phase 1 执行计划，10 个改造项）

### 关键决策
- 继续开发不推倒重来，但冻结纯 UI 开发
- 三线并行：HAL+仿真设备层 / 前端DataStore+UI骨架 / MQTT通信层
- 汇合点：Application::initializeModules() 装配
- 预计 12 个工作日完成 Phase 1

### 仿真方案讨论
- 用户质疑设备仿真的目的、与真实设备差距、对开发任务的具体帮助
- 架构师诚实回答：Stub 能覆盖 60-70% 需求，仿真额外覆盖 30-40%（性能/协同/故障）
- 6 个仿真设备保真度差距大：GPR 高、相机/无人机低
- 修正"上层代码一行不改"承诺：业务逻辑层不改，设备适配层需调整
- 决策：采纳选项 A，仿真分阶段投入（Phase 1 stub → Phase 1.5 回放 → Phase 2 高保真）

### 多 Agent 并行架构调整
- 用户提出多 AI Agent 并行开发需求（单人 + 多 Agent）
- 架构师评估：当前架构最多 2-3 Agent 并行，5 个冲突点（MainWindow/CMake/sim目录/共享类型/数据依赖）
- 5 项架构修改：提取共享类型 + 拆分 MainWindow（UiComponentRegistry）+ 模块化 CMake + 合并改造项2&8 + IDataProvider 解耦
- 引入接口契约机制：docs/contracts/ 目录，Batch 0 冻结
- 修订执行批次：Batch 0（基础设施+契约）→ Batch 1（4并行：HAL/MQTT/Navigation/录制）→ Batch 2（3并行：仿真设备/DataStore/DecisionView）→ Batch 3（集成）→ Batch 4（验证）
- 已落盘 DDR-004，DDR-003 状态改为已确认
