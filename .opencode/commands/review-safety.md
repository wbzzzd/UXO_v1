---
description: 检查安全边界、密钥和文档真实性风险
agent: plan
---

请对当前变更做安全和文档真实性检查，不要修改文件。

检查重点：

- 是否出现明文 provider key、`.env` 内容或凭据。
- `opencode.json` 是否使用环境变量引用密钥。
- 是否把模拟数据描述成真实设备状态。
- 是否实现或触发真实设备控制、外部通信命令、数据库写入或安全关键动作。
- 是否把 SRS/SDD 中尚未实现的模块写成当前事实。
- `.omo/run-continuation/` 等运行态数据是否被纳入提交范围。

输出：

- 严重问题。
- 一般问题。
- 可以继续的条件。
